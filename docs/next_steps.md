# Next steps

The first useful cut is in, and the desk now shows the work. Do the rest one at a time.

1. **Images in the composer** — paste or drop a picture, see a thumbnail, send it with the message. That is the gap the CLI cannot fill.

2. **Keep the thread** — persist the transcript and agent id for the current folder so closing the window does not wipe the day.
