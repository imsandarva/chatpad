# Next steps

The first useful cut is in: login, a folder, a composer, a streamed reply, and Stop. Do these one at a time.

1. **Show the work** — stream tool calls (shell, edits) into the transcript, not only assistant text. The agent already runs them; the desk should show it, like the CLI.

2. **Images in the composer** — paste or drop a picture, see a thumbnail, send it with the message. That is the gap the CLI cannot fill.

3. **Keep the thread** — persist the transcript and agent id for the current folder so closing the window does not wipe the day.
